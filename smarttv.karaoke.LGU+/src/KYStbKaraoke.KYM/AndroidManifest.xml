<?xml version="1.0" encoding="utf-8"?>
<!-- LGU+앱스토어 -->
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="kr.kumyoung.gtvkaraoke"
    android:installLocation="preferExternal"
    android:versionCode="0"
    android:versionName="0.0.0" >

    <!-- <uses-sdk android:minSdkVersion="11" android:targetSdkVersion="12" /> -->
    <!-- 안드로이드 L(Android TV) 대응 관련:Lolli Pop -->
    <uses-sdk
        android:minSdkVersion="17"
        android:targetSdkVersion="21" />

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.CHANGE_NETWORK_STATE" />
    <uses-permission android:name="android.permission.UPDATE_DEVICE_STATS" />
    <uses-permission android:name="android.permission.KILL_BACKGROUND_PROCESSES" /> <!-- process exit -->
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <!-- 해상도지원관련(?) -->
    <!-- <supports-screens android:largeScreens="true" /> -->
    <!-- <supports-screens -->
    <!-- android:anyDensity="true" -->
    <!-- android:largeScreens="true" -->
    <!-- android:normalScreens="true" -->
    <!-- android:resizeable="true" -->
    <!-- android:smallScreens="true" -->
    <!-- android:xlargeScreens="true" /> -->
    <supports-screens
        android:largeScreens="true"
        android:xlargeScreens="true" />

    <!-- 안드로이드 L(Android TV) 대응 관련:Lolli Pop -->
    <!-- 기기기능지원관련 -->
    <uses-feature
        android:name="android.software.leanback"
        android:required="false" />
    <uses-feature
        android:name="com.google.android.tv"
        android:required="false" />
    <uses-feature
        android:name="android.hardware.touchscreen"
        android:required="false" />
    <uses-feature
        android:name="android.hardware.screen.landscape"
        android:required="true" />
    <uses-feature
        android:name="android.hardware.screen.portrait"
        android:required="false" />
    <uses-feature
        android:name="android.hardware.faketouch"
        android:required="false" />
    <uses-feature
        android:name="android.hardware.telephony"
        android:required="false" />
    <uses-feature
        android:name="android.hardware.camera"
        android:required="false" />
    <uses-feature
        android:name="android.hardware.bluetooth"
        android:required="false" />
    <uses-feature
        android:name="android.hardware.nfc"
        android:required="false" />
    <uses-feature
        android:name="android.hardware.gps"
        android:required="false" />
    <uses-feature
        android:name="android.hardware.microphone"
        android:required="false" />
    <uses-feature
        android:name="android.hardware.sensor"
        android:required="false" />

    <application
        android:allowBackup="true"
        android:banner="@drawable/banner"
        android:icon="@drawable/icon_72x72"
        android:isGame="false"
        android:label="@string/app_name"
        android:theme="@style/CustomButton" >

        <!-- 쿼터노트:스트리밍커넥션리시버? -->
        <receiver
            android:name="com.joyul.streaming.ConnReceiver"
            android:enabled="true"
            android:priority="0" >
            <intent-filter>
                <action android:name="android.net.conn.CONNECTIVITY_CHANGE" />
            </intent-filter>
        </receiver>

        <!-- 불가:LGU+라이브업데이트:업데이트서비스를 액티비티에서 실행해야 정상동작한다. -->
        <!-- U+스토어 TVG용 업데이트 Agent_외부연동규격서_v0.3.docx -->
        <!-- <receiver -->
        <!-- android:name="LiveUpdateBroadcastReceiver" -->
        <!-- android:enabled="true" > -->
        <!-- <intent-filter> -->
        <!-- <action android:name="android.lgt.appstore.LIVE_UPDATE_AUTO" /> -->
        <!-- </intent-filter> -->
        <!-- </receiver> -->


        <!-- LGU+셋탑만지원 -->
        <!-- <uses-library android:name="com.lge.iptv" /> -->
        <!-- 스플래쉬화면 -->
        <!-- Lolli Pop -->
        <!-- <activity -->
        <!-- android:name="com.kumyoung.main.SplashActivity" -->
        <!-- android:theme="@android:style/Theme.NoTitleBar" > -->
        <!-- <intent-filter> -->
        <!-- <action android:name="android.intent.action.MAIN" /> -->
        <!-- <category android:name="android.intent.category.LAUNCHER" /> -->
        <!-- 안드로이드 L(Android TV) 대응 관련:Lolli Pop -->
        <!-- <category android:name="android.intent.category.LEANBACK_LAUNCHER" /> -->
        <!-- </intent-filter> -->
        <!-- </activity> -->
        <activity
            android:name="com.kumyoung.gtvkaraoke._ATVKaraokeActivity"
            android:launchMode="singleInstance"
            android:theme="@android:style/Theme.NoTitleBar.Fullscreen" />

        <intent-filter>

            <!-- 안드로이드 L(Android TV) 대응 관련:Lolli Pop -->
            <category android:name="android.intent.category.LEANBACK_LAUNCHER" />
        </intent-filter>
    </application>

</manifest>