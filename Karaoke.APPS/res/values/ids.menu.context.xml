<?xml version="1.0" encoding="utf-8"?>
<resources xmlns:android="http://schemas.android.com/apk/res/android">

    <item name="icon" type="id"/>
    <item name="title" type="id"/>
    <!-- Context Menu List -->
    <!-- 메인화면 (이동) -->
    <!-- <item name="context_home" type="id"/> -->
    <!-- 애창곡 -->
    <item name="context_mysing" type="id"/>
    <!-- 애창곡(등록) -->
    <item name="context_mysing_register" type="id"/>
    <!-- 애창곡(삭제) -->
    <item name="context_mysing_delete" type="id"/>
    <!-- FEEL (재생) -->
    <item name="context_play_feel" type="id"/>
    <!-- SING (재생) -->
    <item name="context_play_sing" type="id"/>
    <!-- LISTEN (재생) -->
    <item name="context_play_listen" type="id"/>
    <!-- FEEL (상세) -->
    <item name="context_feel_view" type="id"/>
    <!-- FEEL (개인) -->
    <item name="context_feel_mylist" type="id"/>
    <!-- FEEL (친구) -->
    <item name="context_feel_ourlist" type="id"/>
    <!-- FEEL (전체) -->
    <item name="context_feel_alllist" type="id"/>
    <!-- FEEL (추천) -->
    <item name="context_feel_hearlist" type="id"/>
    <!-- FEEL (목록) -->
    <item name="context_list_feel" type="id"/>
    <!-- FEEL (탭목록) -->
    <item name="context_tab_feel" type="id"/>
    <!-- 오디션(목록) -->
    <item name="context_list_audition" type="id"/>
    <!-- 오디션(탭목록) -->
    <item name="context_tab_audition" type="id"/>
    <!-- SING (목록) -->
    <item name="context_list_sing" type="id"/>
    <!-- SING (탭목록) -->
    <item name="context_tab_sing" type="id"/>
    <!-- LISTEN (목록) -->
    <item name="context_list_listen" type="id"/>
    <!-- LISTEN (탭목록) -->
    <item name="context_tab_listen" type="id"/>
    <!-- POPSONG(목록) -->
    <item name="context_list_popsong" type="id"/>
    <!-- POPSONG(탭목록) -->
    <item name="context_tab_popsong" type="id"/>
    <!-- 뮤직비디오(YOUTUBE) -->
    <item name="context_list_movie" type="id"/>
    <!-- 뮤직비디오(YOUTUBE) -->
    <item name="context_play_movie" type="id"/>
    <!-- 무료이용 (재생) -->
    <item name="context_free_song" type="id"/>
    <!-- 가사보기 -->
    <item name="context_lylic_info" type="id"/>
    <!-- ARTIST's (이 가수의 다른 곡) -->
    <item name="context_artist_song" type="id"/>
    <!-- SONG's (이 노래의 녹음곡) -->
    <item name="context_song_record" type="id"/>
    <!-- USER's (사용자의 전체 녹음곡) -->
    <item name="context_user_record" type="id"/>
    <!-- 나의 녹음곡 듣기 -->
    <item name="context_my_record" type="id"/>
    <!-- All Recording SONGs -->
    <item name="context_user_all_record" type="id"/>
    <!-- BEST HOLIC -->
    <item name="context_best_holic" type="id"/>
    <!-- 곡소개 -->
    <item name="context_song_info" type="id"/>
    <!-- 녹음곡저장 -->
    <item name="context_record_listen" type="id"/>
    <!-- 녹음곡삭제 -->
    <item name="context_delete_listen" type="id"/>
    <!-- MY BEST -->
    <item name="context_my_best" type="id"/>
    <!-- BEST SONG -->
    <item name="context_best_song" type="id"/>
    <!-- GO HOLIC (회원정보이동) -->
    <item name="context_go_holic" type="id"/>
    <!-- Sing a SONG -->
    <item name="context_sing_a_song" type="id"/>
    <!-- 공유 -->
    <item name="context_share" type="id"/>
    <!-- 팝업화면 -->
    <item name="context_popup" type="id"/>
    <!-- 공지사항(내용) -->
    <item name="context_notice" type="id"/>
    <!-- 쪽지함(내용) -->
    <item name="context_message" type="id"/>
    <!-- 오디션(참여) -->
    <item name="context_audition" type="id"/>
    <!-- 오디션(목록) -->
    <item name="context_audition_list" type="id"/>
    <!-- 오디션(개최) -->
    <item name="context_audition_open" type="id"/>
    <!-- 안드로이드마켓오픈 -->
    <item name="context_android_market_open" type="id"/>
    <!-- 마켓및무료충전오픈 -->
    <item name="context_market_reward_open" type="id"/>
    <!-- 웹뷰오픈 -->
    <item name="context_webview_open" type="id"/>
    <!-- 웹브라우저오픈 -->
    <item name="context_webrowser_open" type="id"/>
    <!-- 신고(웹뷰(오픈 -->
    <item name="context_siren_open" type="id"/>
    <!-- 카카오톡 -->
    <item name="context_kakaotalk" type="id"/>
    <item name="context_kakaotalk_link" type="id"/>
    <item name="context_kakaotalk_story" type="id"/>
    <!-- 공짜존 -->
    <item name="context_freezone" type="id"/>

</resources>