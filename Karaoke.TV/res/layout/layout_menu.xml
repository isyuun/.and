<?xml version="1.0" encoding="utf-8"?>
<LinearLayout
    android:id="@+id/layout_menu"
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@drawable/bg_main_menu"
    android:orientation="vertical">

    <include
        android:id="@+id/layout_menu_main"
        layout="@layout/layout_menu_main"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="10"
        android:visibility="visible"/>

    <include
        layout="@layout/line_solid_menu"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginLeft="@dimen/main_menu_margin_left"
        android:layout_marginRight="@dimen/main_menu_margin_right"
        android:visibility="visible"/>

    <FrameLayout
        android:id="@+id/layout_menu_sub"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="3"
        android:orientation="horizontal">

        <include
            layout="@layout/menu_shop"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:visibility="visible"/>
    </FrameLayout>

</LinearLayout>